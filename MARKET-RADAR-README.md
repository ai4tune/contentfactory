# 市场雷达与 RedFox 使用指南

更新：2026-09-09

## 配置

在项目根目录的 `.env.local` 配置 `REDFOX_API_KEY`，再重启服务。
可选 `REDFOX_HOST=redfox.hk`；如设置 `REDFOX_BASE_URL`，它优先于 HOST，填写站点根地址，不含 `/story/api`。
密钥仅在服务端使用，不提交 Git。接口收费、权限与收录范围以 RedFox 账号为准。

## 页面操作

进入 `/radar`：

- **热榜**：小红书日榜、抖音点赞榜、公众号热门文章。默认昨日，可指定日期与分类/关键词；另有七平台热搜和全网聚合热点 TOP10。
- **主题搜索**：小红书、抖音、公众号、视频号关键词搜索，单次最多 20 条。不会自动扩展关键词或遍历多页。
- **趋势**：同一热搜平台，比较两个相邻的 3/7/14 天完整周期，展示样本平均热度与变化。不是全网文章量或账号增长预测，缺少基期不计算增长率。
- **对标账号**：输入小红书号、抖音号或公众号微信号，保存账号与近期最多 20 篇作品；可手动刷新或停止追踪。不会改变你的当前账号定位，没有后台定时抓取。

作品结果可以收藏到爆款库、创建选题，再结合企业知识库生成简报及多渠道内容。
收藏不自动触发 AI 拆解；数据源未提供正文时，需要在爆款库补充正文后发起拆解。
选题只有保存成内容项目后才进入“已使用”，单纯打开创作页不会改变状态。

## 数据边界

- 热搜是平台热度，不是点赞、播放或阅读，不能混为一个指标。
- 小红书日榜中的 `w+` 数字按近似下界转换，并在榜单注明；未知指标显示“未知”，真实 0 保持 0。
- 公众号账号接口不返回粉丝数；不会编造。
- 视频号当前接入作品搜索；返回的链接可能是视频媒体地址，并非可分享的笔记网页。
- 作品榜支持小红书、抖音和公众号，不把视频号搜索结果冒充视频号官方热榜。
- 账号追踪受数据源收录范围限制，账号 ID 非昵称、非主页链接。
- 外部参考会传入简报，但不会作为企业已验证事实。

## 缓存和调用数量

| 操作 | 缓存有效期 | 每次最多外部调用 |
| --- | --- | --- |
| 主题搜索 | 30 分钟 | 1 |
| 作品榜 | 1 小时 | 1 |
| 平台热搜 | 30 分钟 | 1 |
| 聚合热点 | 10 分钟 | 1 |
| 趋势对比 | 每期 30 分钟 | 2 |
| 添加/刷新对标账号 | 1 小时 | 2 |

命中服务端缓存不重复调用。刷新浏览器不会清除服务端缓存；等待到期即可。
不会自动重试付费请求。业务错误（例如积分不足）会显示，不缓存成成功的空结果。

## 接口与存储

- `POST /api/market/search`
- `POST /api/market/hot`
- `POST /api/market/hotspots`
- `POST /api/market/hot-keywords`
- `POST /api/market/trends`
- `GET/POST /api/market/accounts`
- `POST/DELETE /api/market/accounts/[id]`
- `POST /api/market/items/[id]/save-to-inspiration`

市场条目、追踪账号、响应缓存、调用日志保存在本地 `data/contentfactory.db`。
爆款库和创作项目沿用 V1 存储。停止追踪不会删除已收藏内容及选题。

## 验证

运行 `npm run test:acceptance`，包含插件、V1 API、生产构建和 V2 隔离集成测试。
`V2_BROWSER_VERIFY=1 node src/tests/v2-handoff.test.mjs` 可在已构建后启动临时模拟验收环境；
终端会输出 URL，完成浏览器检查后 Ctrl+C，临时数据会清理。

模拟通过不等于真实账号已开通。真实验收：配置有效 Key，先各做一次搜索、热榜、热搜、账号查询，
核对后台额度、实际返回和页面结果，再决定是否扩大使用量。
详见 [验收记录](docs/acceptance/V2-Handoff-RedFox-Verification.md)。
