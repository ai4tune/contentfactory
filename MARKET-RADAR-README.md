# 市场雷达功能使用指南

## 功能概述

市场雷达是 Content Factory V2 的核心功能之一，帮助企业从外部市场发现机会，结合企业知识生成内容。

## 配置

### 1. 获取 RedFox API Key

1. 访问 [RedFox 官网](https://redfox.hk) 注册账号
2. 获取 API Key
3. 在 `.env.local` 文件中配置：

```bash
REDFOX_API_KEY=your_api_key_here
REDFOX_HOST=redfox.hk  # 可选，默认为 redfox.hk
```

### 2. 验证配置

启动开发服务器后，访问 `/radar` 页面，如果看到"未配置 RedFox API Key"提示，请检查配置。

## 使用方法

### 市场搜索

1. 访问 `/radar` 页面
2. 选择"主题搜索"标签
3. 选择平台（小红书、抖音、公众号）
4. 输入关键词（如：AI获客、内容营销）
5. 点击搜索

### 搜索结果

搜索结果包含：
- 标题和摘要
- 作者信息
- 互动数据（阅读、点赞、收藏、评论）
- 发布时间
- 机会评分

### 保存为灵感

1. 在搜索结果中找到感兴趣的内容
2. 点击"保存为灵感"按钮
3. 内容将保存到爆款库中

### 创建选题

1. 在爆款库中选择内容
2. 点击"创建选题"按钮
3. 系统将自动生成选题

### 开始创作

1. 在选题池中选择选题
2. 点击"开始创作"按钮
3. 系统将跳转到内容创作页面

## 数据缓存

- 搜索结果缓存 6-12 小时
- 热榜数据缓存 30 分钟
- 账号信息缓存 24 小时

## 平台支持

### 小红书
- 搜索：支持关键词搜索
- 热榜：支持单日爆款数据
- 账号：支持账号信息和作品列表

### 抖音
- 搜索：支持关键词搜索
- 热榜：支持点赞排行榜
- 账号：支持账号信息和作品列表

### 公众号
- 搜索：支持关键词搜索
- 热榜：支持热门文章
- 账号：支持账号信息

## 机会评分

系统会根据以下维度计算机会评分：

1. **热度分数** (20%)：基于阅读、点赞、收藏、评论、分享
2. **相对分数** (20%)：相对于作者正常水平的异常程度
3. **受众匹配** (25%)：与目标客户的匹配度
4. **商业价值** (20%)：与企业业务的相关性
5. **知识匹配** (15%)：企业是否有足够材料可以写

## 注意事项

1. **API Key 安全**：RedFox API Key 仅服务端读取，不返回前端
2. **成本控制**：搜索结果会缓存，避免重复调用 API
3. **数据质量**：系统会自动去重，确保数据唯一性
4. **向后兼容**：所有新增字段都是 optional，保证旧数据可继续读取

## 故障排除

### 未配置 RedFox API Key

检查 `.env.local` 文件中是否正确配置了 `REDFOX_API_KEY`。

### 搜索无结果

1. 检查网络连接
2. 检查 API Key 是否有效
3. 尝试其他关键词
4. 检查平台是否支持

### 数据不更新

1. 清除浏览器缓存
2. 检查缓存时间配置
3. 手动刷新页面

## 技术架构

### 数据流

```
用户输入关键词
    ↓
RedFox API 搜索
    ↓
数据标准化
    ↓
去重处理
    ↓
机会评分
    ↓
展示结果
```

### 存储

- 市场项目：SQLite 数据库
- 缓存：SQLite 数据库
- 日志：SQLite 数据库

### API 接口

- `POST /api/market/search` - 市场搜索
- `POST /api/market/items/[id]/save-to-inspiration` - 保存为灵感
- `POST /api/inspirations/[id]/create-idea` - 创建选题
- `POST /api/ideas/[id]/create-project` - 创建内容项目

## 更新日志

### v0.2.0 (2026-09-08)
- 新增市场雷达功能
- 支持小红书、抖音、公众号搜索
- 实现机会评分系统
- 实现数据缓存和去重
- 新增灵感与选题管理
